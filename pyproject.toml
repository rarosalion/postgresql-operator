[project]
name = "postgresql-operator"
version = "0.1.0"
description = "Kubernetes operator that provisions Postgres roles/databases on an external server from PostgresDatabase custom resources."
requires-python = ">=3.12"
dependencies = [
    "kopf>=1.37",
    "kubernetes>=31.0",
    "psycopg2-binary>=2.9",
]
