{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "pep621": {
    "enabled": true
  },
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": ["/^Dockerfile$/"],
      "matchStringsStrategy": "any",
      "matchStrings": [
        "(?<depName>kopf)(?<currentValue>[><=]+[\\d.]+)",
        "(?<depName>kubernetes)(?<currentValue>[><=]+[\\d.]+)",
        "(?<depName>psycopg2-binary)(?<currentValue>[><=]+[\\d.]+)"
      ],
      "datasourceTemplate": "pypi"
    }
  ]
}
