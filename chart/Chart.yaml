apiVersion: v2
name: postgresql-operator
description: Provisions Postgres roles/databases on an external server from PostgresDatabase custom resources.
type: application
version: 0.1.0
appVersion: "0.1.0"
